interface:
  display_name: "WebMind"
  short_description: "Read and operate browser pages through CDP"
  default_prompt: "Use $webmind to read the target webpage, perform any requested browser actions, and verify the result."
