# Mem content index

Template only; no personal experience is included. The `init` and `rebuild-content` commands generate an index for the selected external memory folder.

```text
Mem/
  global.md
  content.md
  example-task/
    memory.md
    flow.md
    ui.md
    rules.md
    notes.md
```

`example-task` demonstrates file formats and is not a learned task. Create meaningful categories only when experience is actually recorded.
