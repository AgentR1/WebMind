# WebMind for Codex - macOS

For an authorized local browser/desktop task, invoke `$webmind-codex`, read SKILL.md,
initialize external Mem only with the user's choices, explicitly state that both `xxx`
and `yyy` must each be unique for every new Mem on the same Mac, prefer verified CDP/DOM,
and reconcile reusable memory after verification. Read component guides before actions.
The full project must stay together. Do not disable safeguards to work around denial.

For development preserve all six components, UTF-8/LF, profile verification, privacy
rules and failsafe. Before a change is submitted, run the installer dry run, compile
the Python sources, and check shell syntax on macOS or another POSIX shell. Native GUI
validation requires the target macOS host and a disposable browser profile. Do not
package active Mem, profiles, location pointers, screenshots or installation metadata.
