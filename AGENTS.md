# WebMind for Codex - Windows

For an authorized local browser/desktop task, invoke `$webmind-codex`, read SKILL.md,
initialize external Mem only with the user's choices, prefer verified CDP/DOM and
reconcile reusable memory after verification. Read component guides before actions.
The full project must stay together. Do not disable safeguards to work around denial.

For development preserve all six components, UTF-8/LF, profile verification, privacy
rules and failsafe. Validate Python syntax, PowerShell parsing, installer dry-run and
documentation links before release. Do not package active Mem, profiles or local reports.
