# WebMind macOS / Codex 使用教程

本教程只适用于 **macOS 上的 Codex**。请下载或克隆完整仓库；代码、启动器、依赖和说明均可独立使用，不需要另一个发行目录。

首次使用前，请先阅读同目录的 [安全说明](安全说明.md)。本项目能够改变网页、账号和桌面状态；安全约束不能保证模型绝不误操作。安装依赖、授予系统权限和接受首次初始化风险是不同事项，不会互相替代。

## 一、项目能做什么

项目包含 CDP 浏览器控制、桌面截图、鼠标、键盘、等待和外部 Mem 六类能力。推荐执行顺序为：读取相关 Mem → 优先使用 CDP/DOM → 必要时使用桌面工具 → 核验结果 → 整理可复用经验。

网页按钮定位、输入和正文读取通常不必先截图：CDP 可以查询 DOM、按选择器定位元素并发送浏览器输入事件。文件选择器、系统对话框、浏览器权限气泡等不属于页面 DOM，需要桌面工具处理。桌面坐标操作必须先观察真实截图，不能只凭历史坐标点击。

本包不包含浏览器本体、已登录 Profile、用户 Mem、Python 虚拟环境或账号凭据。它也不是远程控制服务：需要 Codex、原生 Python 和目标浏览器位于同一台有可见桌面会话的 macOS 设备。

## 二、安装与加载

### 2.1 准备环境

准备本机 Codex、Python 3.10 或更高版本，以及已安装的 Chrome / Chromium / Edge。第一次安装依赖需要联网。请保留完整仓库，不要只复制某个组件，也不要跨机器搬运虚拟环境。

若尚未安装 Codex CLI，可按 [OpenAI 官方 Codex CLI 文档](https://learn.chatgpt.com/docs/codex/cli) 在 macOS 中安装，然后运行 `codex` 并登录：

```bash
curl -fsSL https://chatgpt.com/codex/install.sh | sh
codex
```

也可以把下面的话发给本机 Codex，并提供仓库根目录的真实绝对路径：

```text
请检查并安装这个目录中的 WebMind 项目。先阅读目录中的使用教程和安全说明，确认本机环境后再执行对应安装步骤。安装软件、变更配置或需要额外权限时先征得我同意。不要自动关闭沙箱或更改系统安全策略。
```

### 2.2 手动安装

在 Terminal 中进入完整仓库目录并安装：

```bash
cd "/path/to/webmind-codex"
python3 --version
bash './scripts/install.sh'
bash "$HOME/.agents/skills/webmind-codex/scripts/webmind.sh" doctor --json
```

把 `/path/to/webmind-codex` 替换成仓库的真实路径。默认安装到 `~/.agents/skills/webmind-codex`，运行依赖放在 `~/Library/Application Support/WebMindCodex/.venv`，Mem 仍由首次初始化单独选择。这与 OpenAI 官方文档列出的用户级 Skill 目录一致。[官方 Skill 文档](https://learn.chatgpt.com/docs/build-skills)

安装后用 `$webmind-codex` 显式调用，并确认 Skill 已被发现。Codex 通常会自动检测 Skill 变化；若没有出现，再重新启动 Codex。

### 2.3 后续命令的统一入口

下面的初始化、诊断和浏览器示例使用 `webmind` 作为当前终端中的临时函数。先设置真实路径并定义它；新开终端后需要重新定义。它不是安装脚本额外安装的全局命令。

```bash
WEBMIND_ROOT="$HOME/.agents/skills/webmind-codex"
webmind() { bash "$WEBMIND_ROOT/scripts/webmind.sh" "$@"; }
```

Agent 应从实际发现的 Skill 或插件路径定位根目录，并调用根目录内的完整启动器路径，而不是假定这个临时函数存在。

### 2.4 可选安装参数

本项目的 `scripts/install.py` 支持用户级、项目级及自定义目录安装。下面的参数附加在本版安装命令后：

```text
--scope project --project <项目绝对路径>
--data-dir <外部Python运行目录>
--add-agent-rules
--dry-run
--skip-deps
```

项目级安装放在该项目 `.agents/skills/webmind-codex` 下。`--data-dir` 选择 Python 运行环境等数据目录，并写入安装元数据；它不是选择 Mem 的替代方式。`--add-agent-rules` 只有显式指定时才写入有边界的路由规则，并保留备份；默认不修改全局 Codex 指令或配置。

`--dry-run` 只展示计划，`--skip-deps` 只复制源码，不代表依赖或 GUI 权限已经就绪。不要同时启用多个同名安装而不确认实际命中的路径。自定义运行目录优先使用 `WEBMIND_CODEX_DATA_DIR`；兼容变量 `WEBMIND_DATA_DIR` 也可能影响解析，设置后需核对 `doctor` 的实际路径。

## 三、首次初始化：选择外部 Mem

安装完成不等于已经初始化。第一次具体网页或桌面任务前，Agent 会检查状态，推荐阅读本教程和安全说明，告知仍有风险，并询问你是否接受风险、继续使用。**在你明确同意之前，不应使用 `--accept-risk`。**

你需要选择一个位于 Skill、插件和源码目录之外的父目录。可以是下载、桌面、文档或专门创建的用户数据目录；不要把活动 Mem 放进本发行版目录。选择后，只检查该父目录的直接下一层，不递归扫描整块磁盘。如果发现可加入的 Mem，由你选择是否复用。

新 Mem 的名称必须为 `xxx-yyy-mem`：`xxx` 是 1–8 位小写英文字母，`yyy` 是 1–999 的整数且不能有前导零，`-mem` 后缀不可省略。例如 `work-42-mem` 对应端口 **1042**，因为端口始终为 `1000 + yyy`。

**创建新 Mem 时，`xxx` 和 `yyy` 都必须在这台 Mac 上保持唯一；两者中任何一个都不应与其他 WebMind Mem 重复使用。**

在已经接受风险、选定父目录和名称之后，可以手动执行：

```bash
MEM_PARENT="$HOME/WebMindData"
mkdir -p "$MEM_PARENT"
webmind mem scan --parent "$MEM_PARENT" --json
webmind mem name-info --name work-42-mem --json
webmind mem init --mem-path "$MEM_PARENT/work-42-mem" --accept-risk --json
```

生成的外部目录结构为：

```text
work-42-mem/
  global.md
  content.md
  work-42-mem-Profile/
    webmind-profile.json
    ...浏览器自身数据...
  某类任务/
    memory.md
    flow.md
    ui.md
    rules.md
    notes.md
```

新 Mem 的 `global.md` 从本包内置 `basic-rules.md` 复制；加入现有 Mem 时保留已有 `global.md`。浏览器目录严格为 `<Mem名称>-Profile`，放在对应 Mem 内部。Profile 内的 `webmind-profile.json` 记录绝对路径、回环地址和端口，CDP 在启动或连接前必须先读取并验证。

Skill 内只记录选中的 Mem 位置指针 `components/webmind-mem/mem-location.json`，不保存活动 Mem 或浏览器会话。浏览器 Profile 不参与 Mem 索引、搜索或总结。

请记住 Mem 名字和位置。正常后续任务不应重复询问；更新或重新加载插件后，若位置指针缺失，需要重新选择已有 Mem，不要直接新建后误以为旧经验丢失。需要换 Mem 或专用浏览器时，在任务开始前或结束后重新初始化，不能靠修改 `--endpoint` 或 `--user-data-dir` 临时绕过配置。

**同一台 Mac 上，每个 WebMind Mem 都应使用唯一的 `xxx` 和唯一的 `yyy`，不要复用任何一个值。** 其中 `yyy` 还直接决定调试端口 `1000 + yyy`，因此重复 `yyy` 会造成端口冲突。程序不会通过关闭其他浏览器来解决冲突。

## 四、环境和浏览器检查

依次运行：

```text
webmind doctor --json
webmind mem init-status --json
webmind mem check --json
webmind cdp self-check --json
```

浏览器还没启动时，`cdp self-check` 失败不一定表示安装损坏；这个命令只检查，不会打开浏览器。初始化完成后，可显式启动：

```text
webmind cdp launch --url https://example.com --json
webmind cdp tabs --json
webmind cdp self-check --json
```

Agent 每次执行可能打开浏览器的命令前，应先告知“本次打开的是 CDP（Agent 专用）Chrome”，然后继续任务；这句告知本身不是额外确认。实际浏览器是 Edge 等其他类型时，应使用真实名称。

重点核对 `profile_verified`、实际 Profile 和目标标签页。端口可连接、页面标题正确或启动程序返回成功，都不能单独证明连接到了正确的浏览器。初始化正常、依赖完整、系统权限和浏览器可用也需要分别检查。

### macOS 环境注意事项

本版保留修复后的 LaunchServices 启动路径：CDP 自动调用包内 `launch_chrome_macos.sh`，通过 `open -na <Browser.app> --args ...` 打开专用浏览器，不直接把 Chrome 主进程当作短命令的子进程启动。用户不需要单独调用这个助手。

默认在 `/Applications` 与 `~/Applications` 查找浏览器。自定义路径可以使用 `--chrome-path "/Applications/Google Chrome.app"`，该全局参数放在 CDP 子命令之前。启动就绪默认最多等待 60 秒；`open` 返回成功仍不代表 CDP 就绪。失败时关注错误和私有启动请求日志位置；不要把该日志当成完整浏览器日志，也不要直接转发未经检查的日志内容。

桌面截图需要实际执行命令的 Terminal、IDE 或宿主应用拥有“屏幕录制”权限，鼠标/键盘通常需要“辅助功能”权限。通过系统设置手动授予确有必要的权限，并按系统要求重启宿主。宿主对 shell/CDP 的审批与系统隐私权限是两层不同限制；不要让 Agent 自动修改任何一层。

Apple Silicon 与 Intel 设备应分别使用对应架构的原生 Python，不能互相复制 `.venv`。Retina 截图使用图像像素，而鼠标坐标采用逻辑点；转换为 `x = region.left + image_x / image.scale_x`，y 同理。混合缩放多显示器优先使用单屏范围截图。

Codex 对命令的执行审批仍然有效。本包的 LaunchServices 修复不会自动获得沙箱外权限，也不承诺“每个会话一定只审批一次”。若被拦截，使用当前 Codex 提供的正常、尽可能窄范围的授权流程；不要改用其他壳层规避拒绝。参见 [OpenAI 官方沙箱说明](https://learn.chatgpt.com/docs/sandboxing)。

## 五、标签页与网页操作

浏览器有多个页面时，先运行 `tabs`，取得真实 ID，并为后续动作指定 `--target-id`。示例中的 `TARGET` 必须替换成实际 ID。

```text
webmind cdp --no-auto-launch new-tab --url https://example.com --json
webmind cdp --no-auto-launch switch-tab --target-id TARGET --json
webmind cdp --no-auto-launch close-tab --target-id TARGET --json
webmind cdp --no-auto-launch eval --target-id TARGET --expression "document.title" --json
webmind cdp --no-auto-launch wait-for-selector --target-id TARGET --selector "main" --visible --timeout 10 --json
```

`new-tab` 新建标签页；`switch-tab` 激活已有标签页并请求将其置前，不新建也不关闭；`close-tab` 只关闭指定标签页，拒绝关闭最后一个页面标签，以避免关闭整个浏览器。操作系统仍可能限制窗口抢占焦点，所以进行全局键盘操作前还需验证真实窗口焦点。

`--no-auto-launch` 适合复用已连接且已验证的浏览器。连接丢失时它会失败，而不是悄悄新开窗口。发送、删除、发布、上传等动作之前，仍要确认用户已授权的目标和内容，不能把超时当作“动作没有发生”而重复提交。

### 非敏感中文和复杂文本

优先使用 CDP `fill` / `insert-text`；桌面 `typing type` 只支持可打印 ASCII，不负责中文输入法。`fill` 设置字段并分发输入事件，`insert-text` 通过浏览器输入接口插入文本；复杂网页需要核验实际结果。

Codex 版统一入口支持 UTF-8 `--input-file`，文件可以含中文和多行内容：

```text
webmind cdp --no-auto-launch fill --target-id TARGET --selector "textarea" --input-file <非敏感UTF-8文件绝对路径> --json
webmind cdp --no-auto-launch eval --target-id TARGET --input-file <UTF-8脚本绝对路径> --json
```

`--input-file` 由统一启动器实现，不要直接传给组件脚本。示例路径和选择器需要替换成当前任务中已核实的值。

这些方式只适用于非敏感内容。密码、验证码、令牌、付款资料等不得写入参数、输入文件、日志或剪贴板，必须由你本人在目标页面输入。文件读取和截图不会代替内容授权。

## 六、两种使用模式

### 6.1 推荐：Mem 绑定的专用 CDP 浏览器

它使用电脑上已经安装的浏览器，但采用 Mem 内独立、持久保存的 Profile，而不是日常默认 Profile。适合反复执行网页任务、读取 DOM、减少坐标定位和截图。

```text
$webmind-codex 请使用专用 CDP 浏览器完成任务；开始前读取已选择的 Mem，核验结果后整理经过验证的可复用经验。未经我明确授权，不发送、不删除、不上传、不发布。任务是……
```

第一次访问需要登录的网站时，你可能需要手动登录。网站没有使会话失效、Profile 也未被清理时，后续通常可以复用登录态；不能保证永久免登录。不要把运行中的日常 Profile 直接复制给自动化浏览器。

### 6.2 明确指定：操作你已打开的日常浏览器

这种模式主要使用桌面截图、鼠标和键盘，不把日常默认 Profile 接到 CDP。通常需要更多视觉确认，也更受布局、焦点和显示缩放影响。仍需完成本项目的首次风险确认与 Mem 初始化。

```text
$webmind-codex 本次不要使用 CDP；直接操作我已经打开的日常浏览器。先读取相关 Mem，每次桌面点击前确认真实截图和鼠标位置，结束后整理可复用经验。任务是……
```

不要因为 CDP 与某个网站已经连通，就扩大 Codex 的工作区或宿主权限。对于发送、删除、发布、上传等有外部影响的动作，任务目标和授权范围仍由你决定。

## 七、使用建议

### 从低风险任务开始

可以先测试“打开公开页面并读取标题”“搜索公开文章后总结”“在空白文本编辑器输入测试文字”。首次不要直接操作付款、删除、账号权限或重要文件。

### 给清楚的任务边界

邮件示例：“请在 Gmail 中为指定收件人撰写邀请邮件，只保存草稿，不发送；先核对收件人。”

公开网页示例：“请搜索指定主题的公开文章，读取相关正文并总结；保存到我指定的目录，不登录、不下载可执行文件。”

有外部影响的示例：“请准备提交内容，在最终发送或发布前向我展示目标和内容，等我确认后执行一次。” 原任务授权的范围始终优先，已有登录态不代表可以扩大操作。

### 第一次任务较慢时，可以教模型

模型能力、页面复杂度和网站变化都会影响首次执行。你可以说明输入框与按钮的相对位置、按钮到底是预览还是立即提交、必须先选哪个分类、之后才可填写哪项内容。这些说明不应包含密码、令牌等秘密，也不应扩大已授权范围。

任务结束后，Agent 会审查是否存在经过验证、可复用的经验，并将它们与旧 Mem 合并，删除重复或过时的方法。保留 Mem 并在下一次读取，可能减少重复探索，但不保证每次都更快；“写入记忆”只有在实际写入成功后才算完成。

### 搜索引擎拒绝自动搜索时

搜索查询被拒绝，不等于不能直接打开一个已知官网。你可以手动接管并完成验证；也可以让模型在**同一浏览器和同一 Profile**内使用其他搜索引擎，或直接打开已知官网。不要自动破解 CAPTCHA，不要因此清空登录数据、重建 Profile 或擅自更换控制工具。

### 保持可观察和可中断

桌面操作时不要同时切换窗口或输入文字。保留 PyAutoGUI failsafe；需要停止时，优先使用宿主停止操作或将鼠标移到触发 failsafe 的角落。该机制不等于能撤销已经完成的外部动作，也不保证中止所有 CDP 请求。

## 八、常见故障

| 现象 | 优先检查 |
| --- | --- |
| 提示未初始化 | 运行 `mem init-status`；按风险确认和路径选择流程初始化，不手改指针。 |
| Profile 或端口不匹配 | 检查当前 Mem 名字及 `webmind-profile.json`；不要接管其他浏览器。 |
| 启动后无法连接 | 查看错误、端口冲突、Profile 锁及宿主审批；不要先删 Profile。 |
| 截图成功但点击位置不对 | 核对最新显示器布局、截图区域、图像缩放比例和真实鼠标位置。 |
| 输入落到终端 | 先停止，重新验证目标应用和输入框焦点；不盲目继续按 Enter。 |
| 中文内容乱码 | 核对 UTF-8 文件/管道；使用对应 CDP 文本入口，不用 ASCII 键盘输入法模拟。 |
| 更新后找不到旧经验 | 找回原外部 Mem 并重新初始化选择它，不新建替代后删除旧目录。 |

如果已经发生错误发送、删除、上传或其他重大非预期结果，应立即停在安全位置，只做必要的只读检查并报告，不自动刷新、重试、撤销或清理。

## 九、更新、卸载与备份

升级前结束任务、备份源码修改，并记住外部 Mem 位置。卸载代码不会等于注销网站会话；包含登录状态的 Profile 仍需你自行管理。不要把整个 Mem 当作普通经验文档打包分享，因为它同时含浏览器数据。

可分享的经验也应先检查并脱敏，仅限真正非敏感的任务 Markdown，不要包含 Profile、位置指针、Cookie、缓存、截图或临时输入文件。

## 十、兼容范围与参考

GitHub 发布检查只验证 Python 源码可编译、安装器可生成安装计划、依赖可安装以及 Shell 脚本语法；它不会操作真实桌面，也不能代替目标 Mac 上的权限、显示器、浏览器启动、登录状态和焦点验收。首次使用请从低风险只读任务开始，并核对 `doctor`、`mem init-status`、`mem check` 与 `cdp self-check` 的实际结果。宿主官方资料见 [参考来源](references/SOURCES.md)。
