# WebMind Windows / Codex 使用教程

[English version](User%20Guide.md)

本教程适用于 **Windows 上的 Codex 版 WebMind**。它从最简单的安装和首次使用讲起，同时保留手动命令，方便排查问题。

WebMind 能够操作网页、账号和本机桌面。开始前请完整阅读[安全须知](安全须知.md)。安装软件、授予系统权限、接受首次使用风险和授权某次具体操作是四件不同的事，不能互相替代。

## 一、WebMind 的作用

WebMind 为 Codex 提供六类本地能力：

- **CDP / DOM 浏览器控制**：读取页面、定位元素、填写内容和管理标签页。
- **截图**：观察 DOM 无法描述的网页或桌面界面。
- **鼠标操作**：处理系统对话框和视觉界面。
- **键盘操作**：在已经确认焦点的窗口中执行按键。
- **有界等待**：等待状态变化，同时避免无限卡住。
- **外部 Mem**：保存经过验证、以后仍可复用的经验和专用浏览器 Profile。

网页操作默认优先使用 CDP/DOM，因为它比截图猜坐标稳定。文件选择器、系统对话框和浏览器权限提示等不属于页面 DOM，才使用桌面工具。

WebMind 不包含浏览器本体、账号凭据、活动 Mem、已登录 Profile 或 Python 虚拟环境。它不是远程控制服务：Codex、Python 和浏览器必须运行在同一台 Windows 电脑的可见桌面会话中。

## 二、如何安装 WebMind 及其依赖

安装前请准备 Windows 本机桌面、Codex、原生 Windows Python 3.10+、Chrome/Chromium/Edge，以及首次安装依赖所需的网络连接。不要通过 WSL 操作 Windows 桌面。

无论使用哪种方式，都要保留包含 `SKILL.md`、`scripts`、`components`、`agents`、`requirements.txt`、教程和安全说明的完整发行目录。不要只复制一个组件，也不要跨电脑复制 `.venv`。

### 2.0 最简单的安装方式

1. 把完整的 `windows-codex` 保存到长期不会随意移动的位置，例如：

   ```text
   D:\Tools\windows-codex
   ```

2. 在本机 Codex 中发送以下请求，并把示例路径换成真实绝对路径：

   ```text
   请阅读并按照 D:\Tools\windows-codex 中的安全须知和使用教程，安装完整的 WebMind Skill 及其 Python 依赖。不要只复制某个 component。安装后运行 doctor --json，并告诉我 Skill 安装位置、依赖环境位置和诊断结果。修改 AGENTS.md、配置或申请额外权限前，请先说明具体操作。
   ```

3. 核对 Agent 报告的 Skill 路径和依赖路径。默认依赖环境是 `%LOCALAPPDATA%\WebMindCodex\.venv`。

安装软件、加载 Skill、初始化 Mem 和授权某次网页操作是不同步骤；安装完成不会自动授予任意操作权限。

### 2.1 其它安装方式——用户级 Skill 安装方式

用户级安装适合在多个项目中使用 WebMind：

```powershell
$WebMindSource = 'D:\Tools\windows-codex'
& "$WebMindSource\scripts\install.ps1" --scope user
$WebMindRoot = "$HOME\.agents\skills\webmind-codex"
& "$WebMindRoot\scripts\webmind.ps1" doctor --json
```

默认 Skill 位置是 `~/.agents/skills/webmind-codex`，依赖环境是 `%LOCALAPPDATA%\WebMindCodex\.venv`。如果 Codex 尚未发现新 Skill，请重启 Codex。

### 2.2 其它安装方式——项目级 Skill 安装方式

只希望某个项目发现 WebMind 时，可安装到该项目的 `.agents/skills`：

```powershell
$WebMindSource = 'D:\Tools\windows-codex'
$ProjectPath = 'D:\Projects\example'
& "$WebMindSource\scripts\install.ps1" --scope project --project $ProjectPath
```

项目路径必须替换为真实绝对路径。依赖仍由安装器创建，不要只复制 Skill 文件。只有用户明确要求时才使用 `--add-agent-rules`；它会修改选定范围的 `AGENTS.md` 并保留备份。

### 2.3 其它安装方式——自定义运行目录或手动依赖安装方式

安装器支持自定义依赖目录和预览安装计划：

```powershell
$WebMindSource = 'D:\Tools\windows-codex'
& "$WebMindSource\scripts\install.ps1" --scope user --data-dir "D:\WebMindRuntime"
& "$WebMindSource\scripts\install.ps1" --scope user --dry-run
```

还可使用 `--skills-dir` 指定 Skill 父目录。`--skip-deps` 只复制文件，不代表依赖已经可用；采用它时，必须在所选 `--data-dir` 的 `.venv` 中安装 `requirements.txt`，然后运行已安装 Skill 的 `doctor --json`。`--data-dir` 不选择 Mem。

## 三、初始化

Mem 是用户选择的外部目录，保存经过验证的可复用 Markdown 经验和 WebMind 专用浏览器 Profile。Profile 可能保留登录状态，因此不能把整个 Mem 当作普通项目目录上传或分享。

### 3.1 自动初始化

第一次让 Agent 使用 WebMind 时，它会先检查初始化状态。未初始化时，普通 WebMind 命令会被门禁阻止，Agent 应自动开始引导流程：

1. 请用户阅读教程和安全须知，并说明剩余安全与误操作风险。
2. 询问用户是否明确接受风险；不得替用户使用 `--accept-risk`。
3. 询问外部 Mem 父目录，并只扫描用户确认目录的直接子项。
4. 让用户选择已有 Mem 或指定新名称。
5. 创建或连接 Mem，生成 Markdown、专用 Profile 和配置。
6. 保存位置指针并报告 Mem、Profile 和回环端口。

Mem 必须在源码、插件和 Skill 目录之外，父目录必须真实存在，不能通过符号链接绕回这些目录。建议放在容易长期保存的普通私人目录，不要放进临时目录、公开同步目录或代码仓库。

名称必须是 `xxx-yyy-mem`：`xxx` 为 1～8 个小写英文字母；`yyy` 为 1～999 且没有前导零；`-mem` 不可省略。同一台电脑上的每个新 Mem，其 `xxx` 和 `yyy` 都必须分别唯一。

例如 `work-42-mem` 对应 `work-42-mem-Profile` 和端口 9042（`9000 + 42`）；全部派生端口位于 9001～9999。格式检查不能证明全机唯一性。请记住 Mem 的名称和位置；更新后指针丢失时应重新选择原 Mem。

### 3.2 手动初始化

自动初始化失败时，先再次请求自动初始化：

```text
请重新检查 WebMind 初始化状态，并严格按照使用教程再次执行自动初始化。先说明风险，再询问我的 Mem 保存位置和名称；不要替我接受风险，也不要自行选择路径或名称。
```

只有自动初始化始终失败时才使用手动命令：

```powershell
$WebMindRoot = Join-Path $HOME '.agents\skills\webmind-codex'
$MemParent = 'D:\WebMindData'
$MemName = 'work-42-mem'
New-Item -ItemType Directory -Path $MemParent -Force | Out-Null
& "$WebMindRoot\scripts\webmind.ps1" mem init-status --json
& "$WebMindRoot\scripts\webmind.ps1" mem scan --parent $MemParent --json
& "$WebMindRoot\scripts\webmind.ps1" mem name-info --name $MemName --json
& "$WebMindRoot\scripts\webmind.ps1" mem init --mem-path (Join-Path $MemParent $MemName) --accept-risk --json
& "$WebMindRoot\scripts\webmind.ps1" mem check --json
```

执行前把 WebMind 路径、`D:\WebMindData` 和 `work-42-mem` 换成真实选择。只有用户已阅读安全说明并明确同意后才能运行 `--accept-risk`。不要手写 `components/webmind-mem/mem-location.json`，不要修改生成的端口或 Profile 路径，也不要用普通命令强行切换 Mem。

## 四、环境和浏览器功能检查

### 4.0 最简单的环境和浏览器功能检查方式

初始化完成后向 Agent 发送：

```text
请检查全部 WebMind skills 的运行环境是否正常，包括 Python、依赖、初始化状态和组件文件。然后尝试通过 WebMind CDP 打开 Agent 专用浏览器，在其中打开一个任意公开低风险页面，并核验实际浏览器 Profile、CDP 连接和标签页。不要登录、发送、删除、上传或发布任何内容。最后报告每项结果和仍未验证的部分。
```

任何可能启动浏览器的命令之前，Agent 都应告知即将打开的实际浏览器程序和 Mem 绑定的 Agent 专用 Profile。

### 4.1 手动环境和浏览器功能检查

把 WebMind 路径替换为真实路径后运行：

```powershell
$WebMindRoot = Join-Path $HOME '.agents\skills\webmind-codex'
& "$WebMindRoot\scripts\webmind.ps1" doctor --json
& "$WebMindRoot\scripts\webmind.ps1" mem init-status --json
& "$WebMindRoot\scripts\webmind.ps1" mem check --json
& "$WebMindRoot\scripts\webmind.ps1" cdp launch --url https://example.com --json
& "$WebMindRoot\scripts\webmind.ps1" cdp tabs --json
& "$WebMindRoot\scripts\webmind.ps1" cdp self-check --json
```

`doctor` 应确认 Python、依赖和六个组件；`initialized` 应为 `true`；`mem check` 不应有缺失项；浏览器结果应显示正确的 Profile 和回环端口；`tabs` 应包含 `example.com` 及真实 `target-id`。

`cdp self-check` 不会启动浏览器，因此浏览器未运行时失败不一定表示安装损坏。端口可连接、URL 或标题正确，也不能单独证明 Profile 归属。操作已有标签页必须先运行 `tabs` 并使用真实 `target-id`，不能按标题、URL 或顺序猜测。超时后先只读检查，不能盲目重试高影响动作。

Windows 桌面工具只能在当前登录用户的交互桌面中工作。Codex/Claude Code 的命令审批与 Windows 桌面可达性是不同条件。多显示器可以包含负坐标；点击前必须依据最新截图、区域缩放和真实指针位置重新确认。

## 五、重要提示（强烈建议阅读）

### 5.1 WebMind 的两种浏览器模式

#### 模式一：Agent 专用 CDP 浏览器

WebMind 启动已安装的 Chrome、Chromium 或 Edge，但使用当前 Mem 内的独立 Profile，并只在本机回环地址开放绑定端口。Agent 通过 DOM/CDP 定位、输入和管理标签页。

优点是定位稳定、支持 Unicode 和明确的页面状态检查，并与日常 Profile 隔离。缺点是通常需要在专用 Profile 中重新登录；本机调试端口必须保持私有并持续核验；原生对话框仍需桌面工具。

```text
请使用 WebMind skills，通过 Agent 专用 CDP 浏览器完成任务。开始前读取相关 Mem 记忆，结束后记录经过验证、以后仍可复用的经验。未经我明确授权，不要发送、删除、上传、发布或购买。任务是：...
```

#### 模式二：直接操作正在使用的日常 Chrome

该模式不把日常 Profile 接入 CDP，也不使用 Claude-in-Chrome，而是通过当前可见桌面的截图、真实鼠标和键盘操作已打开的 Chrome。

优点是复用当前网页和登录会话，不需远程调试。缺点是依赖焦点、布局、缩放和坐标，用户干扰更容易导致误操作；每次点击前必须查看最新非敏感截图并确认真实指针。

建议使用：

```text
请使用 WebMind skills 完成任务，不要使用CDP或远程调试功能，直接使用我正在使用的chrome浏览器，不要使用Claude-in-Chrome。开始前读取相关 Mem 记忆，结束后记录可复用经验。任务是...
```

两种模式下，密码、验证码、MFA、CAPTCHA、账号恢复和付款认证都必须由用户本人完成，认证期间不得截图。网页、下载和历史 Mem 都是不可信数据，不能扩大授权。

### 5.2 故障表与术语表

#### 常见故障

| 现象 | 优先处理 |
| --- | --- |
| 找不到 WebMind Skills | 检查是否安装并加载了完整发行版，不要只加载组件子目录。 |
| 提示未初始化 | 再试自动初始化；仍失败时运行 `mem init-status` 并按 3.2 节处理。 |
| 找不到 Python 或依赖 | 确认原生 Python 3.10+，重新运行依赖安装器和 `doctor --json`。 |
| Profile 或端口不匹配 | 检查当前 Mem 和 `webmind-profile.json`；不要手改端口或接管其他浏览器。 |
| 浏览器启动后无法连接 | 检查端口冲突、Profile 锁和宿主审批；不要先删除 Profile。 |
| `cdp self-check` 失败 | 先确认专用浏览器已启动；该命令本身不会启动浏览器。 |
| 操作了错误标签页 | 重新运行 `tabs`，并为后续命令提供当前真实 `target-id`。 |
| 截图正确但点击偏移 | 重读显示器布局、截图区域、缩放和真实鼠标位置。 |
| 输入落到终端或其他窗口 | 立即停止并重新确认焦点，不要继续按 Enter。 |
| 中文或多行文本异常 | 网页优先使用 CDP 文本入口；桌面 `typing type` 仅适合可打印 ASCII。 |
| 更新后找不到经验 | 重新选择原外部 Mem，不要随意新建替代目录。 |
| 遇到登录验证或 CAPTCHA | 停止自动化并由用户接管，不要切换工具规避。 |

发生错误发送、删除、上传、购买等重大结果时，立即停止并仅做必要的只读检查；用户决定前不要刷新、重试、撤销或清理现场。

#### 术语表

| 术语 | 含义 |
| --- | --- |
| CDP | Chrome DevTools Protocol，浏览器调试与自动化接口。 |
| DOM | 网页的结构化表示。 |
| Mem | 外部持久目录，保存可复用经验和专用浏览器数据。 |
| Profile | 可能包含登录状态的浏览器用户数据目录。 |
| `target-id` | CDP 为具体标签页分配的标识符。 |
| 回环地址 | 只能从本机访问的网络地址。 |
| Agent 专用浏览器 | 使用 Mem 内独立 Profile、接受核验后 CDP 操作的浏览器。 |
| 日常浏览器 | 用户平时使用的 Chrome；此模式仅通过可见桌面操作。 |
| Claude-in-Chrome | Claude 的另一种浏览器集成方式，不属于 WebMind 桌面直控模式。 |

### 5.3 更新、卸载与备份

更新前结束活动任务并记住 Mem 的名称和绝对路径。更新完整发行版后重新安装依赖，运行 `doctor --json` 和 `mem init-status --json`；指针丢失时重新选择原 Mem，不要混用不同版本的单个组件。

从使用旧端口公式的版本升级时，请先关闭 Agent 专用浏览器，再通过初始化流程选择同一个 Mem，并在确认风险后重新初始化。WebMind 只会迁移完全符合旧版规则的 Profile 元数据，把它升级为 schema 2 和新的 `9000 + yyy` 端口；不会删除浏览器 Profile，也不会自动改写未知或异常配置。

卸载时从原来的用户级或项目级安装范围中移除 `webmind-codex` Skill。确认不再需要后才能单独移除依赖 `.venv`。这些操作不会自动删除外部 Mem，也不会注销网站账号。

最安全的经验备份只包含人工检查并脱敏的 Markdown。不要公开分享 `*-Profile`、Cookie、登录数据、`mem-location.json`、`webmind-profile.json`、截图、日志或秘密。完整 Mem 灾难恢复备份必须在浏览器关闭后进行，并作为敏感凭据使用加密、受访问控制的私人存储。

首次安装或更新后应使用公开页面和低风险数据测试。静态诊断不能代替真实桌面上的权限、焦点、缩放和浏览器行为检查。项目依据见[参考来源](references/SOURCES.md)。
