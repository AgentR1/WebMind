# WebMind Windows / Claude Code 使用教程

[English version](USER_GUIDE.md)

本教程适用于 **Windows 上的 Claude Code 版 WebMind**。它从最简单的安装和首次使用讲起，同时保留手动命令，方便排查问题。

WebMind 能够操作网页、账号和本机桌面。开始前请完整阅读[安全须知](安全须知.md)。安装软件、授予系统权限、接受首次使用风险和授权某次具体操作是四件不同的事，不能互相替代。

## 一、WebMind 的作用

WebMind 为 Claude Code 提供六类本地能力：

- **CDP / DOM 浏览器控制**：读取页面、定位元素、填写内容和管理标签页。
- **截图**：观察 DOM 无法描述的网页或桌面界面。
- **鼠标操作**：处理系统对话框和视觉界面。
- **键盘操作**：在已经确认焦点的窗口中执行按键。
- **有界等待**：等待状态变化，同时避免无限卡住。
- **外部 Mem**：保存经过验证、以后仍可复用的经验和专用浏览器 Profile。

网页操作默认优先使用 CDP/DOM，因为它比截图猜坐标稳定。文件选择器、系统对话框和浏览器权限提示等不属于页面 DOM，才使用桌面工具。

WebMind 不包含浏览器本体、账号凭据、活动 Mem、已登录 Profile 或 Python 虚拟环境。它不是远程控制服务：Claude Code、Python 和浏览器必须运行在同一台 Windows 电脑的可见桌面会话中。

## 二、如何安装 WebMind 及其依赖

安装前请确认电脑符合以下要求：

- Windows 10 或 Windows 11，并且正在使用本机可见桌面；不要通过 WSL 操作 Windows 桌面。
- 已安装 Claude Code。
- 已安装原生 Windows Python 3.10 或更高版本。
- 已安装 Chrome、Chromium 或 Edge。
- 首次安装 Python 依赖时可以连接网络。

无论采用哪种安装方式，都必须保留当前发行版的完整目录。目录中至少应包含 `.claude-plugin`、`scripts`、`skills`、`requirements.txt`、使用教程和安全须知。不要只复制某一个 Skill，也不要从另一台电脑复制 Python 虚拟环境 `.venv`。

### 2.0 最简单的安装方式

1. 将完整的 `windows-claudecode` 项目目录保存到一个长期不会随意移动的位置，例如：

   ```text
   D:\Tools\windows-claudecode
   ```

   如果下载的是压缩包，应先完整解压。示例路径可以换成自己的路径，但不要只保存 `skills` 或其中某个组件。

2. 在这台电脑上的 Claude Code 中发送下面的请求，并把示例路径替换为项目的真实绝对路径：

   ```text
   请阅读并按照 D:\Tools\windows-claudecode 中的安全须知和使用教程，安装完整的 WebMind plugin、全部 WebMind skills 及其 Python 依赖。不要只安装单个 skill。安装后运行环境诊断，并告诉我安装位置、依赖环境位置和诊断结果。修改配置或申请额外权限前，请先说明具体操作。
   ```

3. Agent 完成后，检查它报告的路径与自己保存项目的位置是否一致。默认依赖环境位于：

   ```text
   %LOCALAPPDATA%\WebMind\.venv
   ```

这种方式最适合不希望手动输入命令的用户。安装依赖、加载插件、初始化 Mem 和授权具体网页操作仍然是不同的步骤；完成安装并不代表 Agent 已获得任意操作权限。

### 2.1 其它安装方式——本地目录临时加载安装方式

这种方式直接从保存的项目目录加载插件，适合首次试用和开发调试。

在原生 Windows PowerShell 中运行：

```powershell
$WebMindRoot = 'D:\Tools\windows-claudecode'
& "$WebMindRoot\scripts\install.ps1"
claude --plugin-dir "$WebMindRoot"
```

其中：

- `install.ps1` 会在 `%LOCALAPPDATA%\WebMind\.venv` 创建 Python 虚拟环境，并安装 `requirements.txt` 中的 Pillow、mss 和 PyAutoGUI。
- `claude --plugin-dir` 只在本次 Claude Code 会话中加载该插件；关闭会话后，下次需要再次使用相同参数启动。
- 如果设置了 `WEBMIND_DATA_DIR`，依赖环境会安装到该目录下的 `.venv`，但这个变量不会选择或切换 Mem。

安装依赖后可运行：

```powershell
& "$WebMindRoot\scripts\webmind.ps1" doctor --json
```

### 2.2 其它安装方式——Claude Code marketplace 安装方式

这种方式适合希望由 Claude Code 持久管理插件的用户。项目已经包含 `.claude-plugin/plugin.json` 和 marketplace 清单。

从 GitHub 仓库安装时，可按实际仓库地址运行：

```text
claude plugin marketplace add <GitHub用户名>/<仓库名>
claude plugin install webmind-claudecode@webmind-claudecode
```

也可以把完整的本地插件根目录作为 marketplace 来源。不要同时从多个来源启用同一个 WebMind 插件。

marketplace 通常使用缓存后的插件副本，因此安装 Python 依赖时，应让 Agent 先定位当前实际启用的 `webmind-claudecode` 插件根目录，再运行其中的 `scripts\install.ps1`：

```text
请定位当前实际启用的 webmind-claudecode 插件根目录，运行该目录中的 scripts\install.ps1 安装依赖，然后运行 scripts\webmind.ps1 doctor --json。不要根据旧路径猜测插件位置。
```

### 2.3 其它安装方式——手动创建 Python 环境安装方式

当 `install.ps1` 无法运行时，先检查是否只是路径写错或 Python 未安装。不要为了运行脚本而降低 PowerShell 的全局执行策略。确认需要手动安装后，可执行以下等价步骤：

```powershell
$WebMindRoot = 'D:\Tools\windows-claudecode'
$WebMindData = if ($env:WEBMIND_DATA_DIR) {
    $env:WEBMIND_DATA_DIR
} else {
    Join-Path $env:LOCALAPPDATA 'WebMind'
}
$WebMindVenv = Join-Path $WebMindData '.venv'
$WebMindPython = Join-Path $WebMindVenv 'Scripts\python.exe'

py -3 -c "import sys; raise SystemExit(0 if sys.version_info >= (3, 10) else 1)"
py -3 -m venv $WebMindVenv
& $WebMindPython -m pip install --upgrade pip
& $WebMindPython -m pip install -r "$WebMindRoot\requirements.txt"
& $WebMindPython "$WebMindRoot\scripts\webmind.py" doctor --json
```

如果电脑没有 `py` 启动器，但 `python` 指向 Python 3.10 或更高版本，可将上面的 `py -3` 替换为 `python`。依赖安装完成后，仍需通过 `claude --plugin-dir "$WebMindRoot"` 或 marketplace 加载插件。

## 三、初始化

WebMind 使用一个由用户选择的外部 Mem。Mem 同时保存经过验证的可复用经验，以及 WebMind 专用浏览器的独立 Profile。Profile 可能保留网站登录状态，所以 Mem 不是普通的项目文档目录，不能随意上传或分享。

### 3.1 自动初始化

第一次让 Agent 使用任意 WebMind Skill 执行任务时，Agent 会先检查初始化状态。尚未初始化时，正常的 WebMind 命令会被阻止，Agent 应自动进入初始化流程，而不是绕过检查。

自动初始化通常会依次进行以下步骤：

1. 提醒用户阅读本教程和[安全须知](安全须知.md)，说明 WebMind 仍然存在安全与误操作风险。
2. 询问用户是否明确接受剩余风险。未经用户同意，Agent 不得自行添加 `--accept-risk`。
3. 询问外部 Mem 的父目录。Agent 可以检查用户确认的父目录，但不会递归搜索整台电脑。
4. 列出该父目录下已有的 Mem，让用户选择已有 Mem，或者让用户指定一个新名称。
5. 创建或连接 Mem，生成必要的 Markdown 文件、专用浏览器 Profile 和 Profile 配置。
6. 保存 Mem 的位置指针，并报告 Mem 路径、Profile 路径和本机 CDP 端口。

保存路径必须满足以下要求：

- Mem 必须位于 WebMind 源码、Claude Code 插件目录和各个 Skill 目录之外。
- 建议选择自己容易找到、可以长期保留的普通用户目录，例如 `D:\WebMindData`、桌面、文档或下载目录。
- 父目录必须真实存在。不要把 Mem 放进临时目录、同步公开分享的目录或代码仓库。
- 不要通过符号链接或目录联接把外部路径绕回插件目录。

Mem 名称必须采用：

```text
xxx-yyy-mem
```

其中：

- `xxx` 是 1～8 个小写英文字母。
- `yyy` 是 1～999 的整数，不能有前导零。
- `-mem` 不可省略。
- 在同一台电脑上，每个新 Mem 的 `xxx` 和 `yyy` 都必须分别唯一；不能只保证完整名称不同。

例如 `work-42-mem` 会使用 `work-42-mem-Profile`，并使用本机回环端口 `9042`，计算方式为 `9000 + 42`；全部派生端口位于 9001～9999。格式检查只能判断名称是否合法，不能自动证明整台电脑上的唯一性。

自动初始化完成后，请记住 Mem 的名称和保存位置。WebMind 更新后如果位置指针丢失，应重新选择原来的 Mem，而不是直接新建一个并丢弃旧经验。

### 3.2 手动初始化

自动初始化没有完成时，应先告诉 Agent 再次执行自动初始化：

```text
请重新检查 WebMind 的初始化状态，并严格按照使用教程执行自动初始化。先说明风险，再询问我的 Mem 保存位置和名称；不要替我接受风险，也不要自行选择路径或名称。
```

只有在多次尝试后自动初始化始终失效，才采用下面的手动方式。

```powershell
$WebMindRoot = 'D:\Tools\windows-claudecode'
$MemParent = 'D:\WebMindData'
$MemName = 'work-42-mem'

New-Item -ItemType Directory -Path $MemParent -Force | Out-Null
& "$WebMindRoot\scripts\webmind.ps1" mem init-status --json
& "$WebMindRoot\scripts\webmind.ps1" mem scan --parent $MemParent --json
& "$WebMindRoot\scripts\webmind.ps1" mem name-info --name $MemName --json
& "$WebMindRoot\scripts\webmind.ps1" mem init --mem-path (Join-Path $MemParent $MemName) --accept-risk --json
& "$WebMindRoot\scripts\webmind.ps1" mem check --json
```

执行前必须替换：

- 把 `D:\Tools\windows-claudecode` 替换为完整 WebMind 项目的真实绝对路径。
- 把 `D:\WebMindData` 替换为用户自己选择的外部父目录。
- 把 `work-42-mem` 替换为符合规则、且 `xxx` 与 `yyy` 在本机分别唯一的名称。

只有在用户已经阅读安全须知并明确同意继续后，才可以执行带有 `--accept-risk` 的最后初始化命令。不要手动编写 `skills\webmind-mem\mem-location.json`，不要修改自动生成的端口或 Profile 路径，也不要用普通命令的 `--mem-path` 参数强行切换 Mem。

## 四、环境和浏览器功能检查

环境检查只能证明依赖和部分配置可用；真实浏览器、桌面焦点、显示器缩放与系统权限仍需通过低风险操作验证。请先完成初始化，再测试浏览器。

### 4.0 最简单的环境和浏览器功能检查方式

向已经加载 WebMind 的 Agent 发送：

```text
请检查全部 WebMind skills 的运行环境是否正常，包括 Python、依赖、初始化状态和组件文件。然后尝试通过 WebMind CDP 打开 Agent 专用浏览器，在其中打开一个任意的公开低风险页面，并核验实际浏览器 Profile、CDP 连接和标签页。不要登录、发送、删除、上传或发布任何内容。最后向我报告每项检查结果和仍未验证的部分。
```

Agent 在可能启动浏览器前，应明确告知将打开实际检测到的浏览器程序和 Mem 绑定的“Agent 专用 CDP Profile”。这是一项提示，不代表用户授权了提示之外的其他网页操作。

### 4.1 手动环境和浏览器功能检查

将第一行路径替换为真实项目路径，然后在原生 Windows PowerShell 中运行：

```powershell
$WebMindRoot = 'D:\Tools\windows-claudecode'

& "$WebMindRoot\scripts\webmind.ps1" doctor --json
& "$WebMindRoot\scripts\webmind.ps1" mem init-status --json
& "$WebMindRoot\scripts\webmind.ps1" mem check --json
& "$WebMindRoot\scripts\webmind.ps1" cdp launch --url https://example.com --json
& "$WebMindRoot\scripts\webmind.ps1" cdp tabs --json
& "$WebMindRoot\scripts\webmind.ps1" cdp self-check --json
```

检查结果时重点关注：

- `doctor` 应显示正确的 Python 版本，并确认 Pillow、mss、PyAutoGUI 和五个可执行组件存在。Claude Code 版的 wait 是工作流 Skill，不是统一启动器中的第六个命令。
- `mem init-status` 的 `initialized` 应为 `true`，`mem check` 不应报告缺失文件。
- 浏览器启动和连接结果应显示 Mem 对应的 Profile 路径和回环端口，并通过 Profile 核验。
- `tabs` 应列出刚刚打开的 `https://example.com` 页面及其真实 `target-id`。

`cdp self-check` 本身不会启动浏览器；浏览器尚未运行时，该命令失败不一定表示安装损坏。端口能够连接、页面标题正确或 URL 正确，也不能单独证明连接的是正确 Profile。

如需继续手动验证 DOM，可从 `tabs` 输出复制真实标签页 ID，替换下面的 `TARGET`：

```powershell
& "$WebMindRoot\scripts\webmind.ps1" cdp --no-auto-launch eval --target-id TARGET --expression "document.title" --json
& "$WebMindRoot\scripts\webmind.ps1" cdp --no-auto-launch wait-for-selector --target-id TARGET --selector "body" --visible --timeout 10 --json
```

每个针对已有标签页的命令都必须使用真实 `target-id`，不要根据标题、URL 或列表顺序猜测标签页。超时也不代表动作一定没有发生；涉及外部影响的操作超时后，必须先进行只读检查，不能盲目重试。

## 五、重要提示（强烈建议阅读）

### 5.1 WebMind 的两种浏览器模式

WebMind 可以用两种方式操作浏览器。这两种模式的原理、能力和风险不同，开始任务前应明确告诉 Agent 使用哪一种。

#### 模式一：Agent 专用 CDP 浏览器

WebMind 启动电脑上已经安装的 Chrome、Chromium 或 Edge 程序，但使用保存在当前 Mem 中的独立 Profile，并只在本机回环地址开放与该 Mem 绑定的 CDP 端口。Agent 通过页面 DOM 和 Chrome DevTools Protocol 定位元素、输入内容和管理标签页。

优点：

- DOM 定位通常比截图猜坐标更准确、更稳定。
- 支持 Unicode 输入、标签页管理和明确的页面状态检查。
- 与日常浏览器 Profile 隔离，降低误操作日常标签页的概率。

缺点：

- 与日常 Chrome 的登录状态通常不共享，需要在专用 Profile 中由用户亲自登录。
- 浏览器开启了仅限本机使用的远程调试接口，因此必须保持 Profile 和端口核验，不得公开或转发端口。
- 文件选择器、浏览器权限提示和系统对话框仍可能需要桌面截图、鼠标或键盘。

使用该模式时，可以发送：

```text
请使用 WebMind skills，通过 Agent 专用 CDP 浏览器完成任务。开始前读取相关 Mem 记忆，结束后记录经过验证、以后仍可复用的经验。未经我明确授权，不要发送、删除、上传、发布或购买。任务是：...
```

#### 模式二：直接操作正在使用的日常 Chrome

该模式不把日常 Chrome Profile 接入 CDP，也不使用 Claude-in-Chrome。WebMind 通过当前可见桌面的截图、真实鼠标和键盘操作用户已经打开的 Chrome。

优点：

- 可以直接复用用户当前打开的网页和已经登录的日常会话。
- 不需要为该网站在 Agent 专用 Profile 中重新登录。
- 不需要给日常 Chrome 开启远程调试。

缺点：

- 主要依赖窗口位置、焦点、显示缩放和截图坐标，比 DOM 模式脆弱。
- 自动化期间用户切换窗口、移动鼠标或输入内容，可能导致操作落到错误位置。
- 每次桌面点击前都必须移动到目标、截取非敏感画面、查看真实鼠标位置并确认；中文网页输入通常也不如 CDP 方便。

如果希望 WebMind 直接操作自己正在使用的 Chrome，建议使用下面这段命令：

```text
请使用 WebMind skills 完成任务，不要使用CDP或远程调试功能，直接使用我正在使用的chrome浏览器，不要使用Claude-in-Chrome。开始前读取相关 Mem 记忆，结束后记录可复用经验。任务是...
```

无论采用哪一种模式，登录密码、验证码、MFA、CAPTCHA、账号恢复和付款认证都必须由用户本人完成。认证期间不得截图。网页内容、下载文件和历史 Mem 都属于不可信数据，不能替代用户授权或要求 Agent 关闭安全限制。

### 5.2 故障表与术语表

#### 常见故障

| 现象 | 优先检查与处理方式 |
| --- | --- |
| 找不到 WebMind Skills | 确认完整插件已通过 `--plugin-dir` 或 marketplace 加载，不要只加载某个 `skills` 子目录。 |
| 提示尚未初始化 | 先让 Agent 再次执行自动初始化；仍然失败时运行 `mem init-status`，再按 3.2 节手动初始化。 |
| 找不到 Python 或依赖 | 确认是原生 Windows Python 3.10+，重新运行 `scripts\install.ps1`，然后执行 `doctor --json`。 |
| `webmind` 不是可识别命令 | 教程中的 `webmind` 只是简称；手动操作时应使用 `& "$WebMindRoot\scripts\webmind.ps1" ...`。 |
| Profile 或端口不匹配 | 核对当前 Mem 与 `webmind-profile.json`；不要手改端口，不要连接或接管其他浏览器。 |
| 浏览器启动后无法连接 | 检查 Profile 是否被其他进程占用、端口是否冲突以及 Claude Code 是否拒绝了必要命令；不要先删除 Profile。 |
| `cdp self-check` 失败 | 先确认专用浏览器是否已经启动；该检查不会自动启动浏览器。 |
| 找不到标签页或操作错标签页 | 重新运行 `tabs --json`，给后续命令提供当前真实 `target-id`。 |
| 截图正常但点击位置偏移 | 重新读取显示器布局和截图元数据，检查缩放、截图区域、负坐标与真实鼠标位置。 |
| 输入内容落到终端或其他窗口 | 立即停止；重新确认目标应用、窗口焦点、输入光标和输入法状态，不要继续按 Enter。 |
| 中文或多行输入异常 | 网页优先使用 CDP `fill` 或 `insert-text`；桌面 `typing type` 只适合可打印 ASCII。 |
| 更新后找不到旧经验 | 找回原来的外部 Mem，并重新执行初始化选择它；不要为了省事新建另一个 Mem。 |
| CAPTCHA、MFA 或登录验证阻塞 | 停止自动化，由用户亲自完成；不要清空 Profile、切换工具或自动规避验证。 |

如果已经发生错误发送、删除、上传、购买或其他重大非预期结果，应立即停止，只做必要的只读检查。在用户作出决定前，不要刷新、重试、撤销、删除证据或清理现场。

#### 术语表

| 术语 | 含义 |
| --- | --- |
| WebMind Skill | 告诉 Agent 何时以及如何使用某一项 WebMind 能力的说明和工具集合。 |
| CDP | Chrome DevTools Protocol，Chrome 系浏览器提供的调试和自动化接口。 |
| DOM | 网页的结构化表示，Agent 可据此定位按钮、输入框和页面内容。 |
| Mem | 位于插件之外的持久目录，保存可复用经验和对应的专用浏览器 Profile。 |
| Profile | 浏览器用户数据目录，可能包含历史记录和登录状态，应视为敏感数据。 |
| `target-id` | CDP 为具体标签页分配的标识符，针对已有标签页的命令必须明确提供。 |
| 回环地址 | 只能从本机访问的网络地址，本项目的 CDP 端口只能绑定回环地址。 |
| Agent 专用浏览器 | 使用 Mem 内独立 Profile、允许 WebMind 通过经过核验的 CDP 操作的浏览器实例。 |
| 日常浏览器 | 用户平时正在使用的 Chrome；WebMind 仅通过可见桌面操作，不应把它临时接入 CDP。 |
| Claude-in-Chrome | Claude 的另一种浏览器集成方式，不属于本教程所述的 WebMind 桌面直控模式。 |

### 5.3 更新、卸载与备份

#### 更新

更新前应结束正在执行的 WebMind 任务，并记住当前外部 Mem 的名称和绝对路径。

- 本地目录加载：使用新版本完整替换或更新项目文件，然后重新运行 `scripts\install.ps1`。不要把新旧版本的单个 Skill 混合在一起。
- marketplace 安装：通过 Claude Code 的插件管理功能更新插件，再定位更新后的实际插件根目录并重新运行其中的 `scripts\install.ps1`。
- 更新后运行 `doctor --json` 和 `mem init-status --json`。如果位置指针未保留，应通过初始化流程重新选择原 Mem。

从使用旧端口公式的版本升级时，请先关闭 Agent 专用浏览器，再通过初始化流程选择同一个 Mem，并在确认风险后重新初始化。WebMind 只会迁移完全符合旧版规则的 Profile 元数据，把它升级为 schema 2 和新的 `9000 + yyy` 端口；不会删除浏览器 Profile，也不会自动改写未知或异常配置。

不要用旧版本中的 `mem-location.json`、`webmind-profile.json` 或单个脚本覆盖新版本文件。切换版本前先检查发行说明和路径差异。

#### 卸载

- 使用 `claude --plugin-dir` 临时加载时，关闭该 Claude Code 会话并且以后不再添加 `--plugin-dir`，插件就不会继续加载。
- 使用 marketplace 安装时，通过 Claude Code 的插件管理命令卸载 `webmind-claudecode@webmind-claudecode`。
- 确认不再需要运行环境后，可以单独删除 `%LOCALAPPDATA%\WebMind\.venv`，或删除自己通过 `WEBMIND_DATA_DIR` 指定的对应 `.venv`。
- 删除插件或 Python 环境不会自动删除外部 Mem，也不会替用户注销网站账号。

不要在没有确认路径和备份的情况下删除整个 Mem。若决定永久删除包含登录状态的 Profile，应先关闭使用该 Profile 的浏览器，并明确确认删除目标。

#### 备份

最安全的经验备份方式，是只备份经过人工检查和脱敏的 Markdown 文件，例如 `global.md`、`content.md` 和任务目录中的 `.md` 文件。

不要公开上传或分享：

- `*-Profile` 浏览器目录及其中的 Cookie、登录数据或会话信息。
- `mem-location.json` 和 `webmind-profile.json`。
- 截图、日志、临时输入文件、密码、验证码、令牌、支付资料或私钥。

如果为了本机灾难恢复而完整备份 Mem，应先关闭对应浏览器，把备份当作敏感凭据保存，使用受访问控制和加密的私人存储，并且绝不把它提交到代码仓库或发送给他人。

完成安装、初始化或更新后，建议先用公开页面和一次性低风险数据测试。静态诊断不能代替真实 Windows 桌面上的权限、焦点、显示缩放和浏览器行为检查。项目依据和宿主参考见[参考来源](references/SOURCES.md)。
