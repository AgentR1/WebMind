# WebMind Windows / Claude Code 使用教程

[English version](USER_GUIDE.md)

本教程适用于 **Windows 上的 Claude Code 版 WebMind**。它从最简单的安装和首次使用讲起，同时保留手动命令，方便排查问题。

WebMind 能够操作网页、账号和本机桌面。开始前请完整阅读[安全须知](安全须知.md)。安装软件、授予系统权限、接受首次使用风险和授权某次具体操作是四件不同的事，不能互相替代。

## 一、先看快速流程

第一次使用时，依次完成：

1. 阅读安全须知。
2. 准备 Claude Code、Python 3.10+ 和 Chrome、Chromium 或 Edge。
3. 安装完整的 WebMind Skill，不要只复制单个组件。
4. 选择项目外部的 Mem，并明确接受剩余风险。
5. 运行诊断，启动并核验专用浏览器 Profile。
6. 从公开网页的只读任务开始测试。

## 二、WebMind 能做什么

WebMind 为 Claude Code 提供六类本地能力：

- **CDP / DOM 浏览器控制**：读取页面、定位元素、填写内容和管理标签页。
- **截图**：观察 DOM 无法描述的网页或桌面界面。
- **鼠标操作**：处理系统对话框和视觉界面。
- **键盘操作**：在已经确认焦点的窗口中执行按键。
- **有界等待**：等待状态变化，同时避免无限卡住。
- **外部 Mem**：保存经过验证、以后仍可复用的经验和专用浏览器 Profile。

网页操作默认优先使用 CDP/DOM，因为它比截图猜坐标稳定。文件选择器、系统对话框和浏览器权限提示等不属于页面 DOM，才使用桌面工具。

WebMind 不包含浏览器本体、账号凭据、活动 Mem、已登录 Profile 或 Python 虚拟环境。它不是远程控制服务：Claude Code、Python 和浏览器必须运行在同一台 Windows 电脑的可见桌面会话中。

## 三、开始前的准备

请确认已有：Windows 10/11、Claude Code、原生 Windows Python 3.10+、Chrome/Chromium/Edge，以及首次安装依赖所需的网络连接。

不要从 WSL 操作 Windows 桌面。如果下载的是压缩包，请完整解压；不要只复制 `components` 中的一个目录，也不要跨电脑复制 `.venv`。

## 四、安装 WebMind

### 4.1 让 Claude Code 协助安装

向本机 Claude Code 提供插件根目录的真实绝对路径：

```text
请帮我检查位于 "D:\Tools\windows-claudecode" 的 WebMind。先阅读安全须知和使用教程，再安装依赖。修改配置或申请额外权限前，请说明具体操作并征得我同意。
```

### 4.2 从本地源码安装

必须保留包含 `.claude-plugin`、`scripts` 和 `skills` 的完整目录：

```powershell
$WebMindRoot = 'D:\Tools\windows-claudecode'
& "$WebMindRoot\scripts\install.ps1"
& "$WebMindRoot\scripts\webmind.ps1" doctor --json
claude --plugin-dir "$WebMindRoot"
```

默认 Python 环境位于 `%LOCALAPPDATA%\WebMind\.venv`。`--plugin-dir` 只为当前 Claude Code 会话加载插件，安装依赖并不会自动注册插件。

若 `.ps1` 被系统阻止，不要降低执行策略；按 `scripts/install.ps1` 的等价步骤用原生 Python 创建虚拟环境、安装 `requirements.txt`，再运行 `scripts/webmind.py doctor --json`。

### 4.3 定义教程中的 `webmind` 简写

```powershell
$WebMindRoot = 'D:\Tools\windows-claudecode'
$WebMindData = if ($env:WEBMIND_DATA_DIR) { $env:WEBMIND_DATA_DIR } else { Join-Path $env:LOCALAPPDATA 'WebMind' }
$WebMindPython = Join-Path $WebMindData '.venv\Scripts\python.exe'
function webmind { & $WebMindPython "$WebMindRoot\scripts\webmind.py" @args }
```

### 4.4 可选：通过 marketplace 持久安装

发布到 GitHub 后：

```text
claude plugin marketplace add <GitHub用户名>/<仓库名>
claude plugin install webmind-claudecode@webmind-claudecode
```

也可以用插件根目录作为 marketplace 来源。不要从多个来源重复启用同一插件。marketplace 使用缓存副本，因此运行时应从实际 Skill 路径定位插件根目录。`WEBMIND_DATA_DIR` 只改变 Python 运行数据位置，不选择 Mem。

## 五、首次初始化 Mem

Mem 是由用户选择的外部目录，同时保存可复用的 Markdown 经验和 WebMind 专用浏览器 Profile。因为 Profile 可能保留登录状态，Mem 必须位于源码、Skill 和插件目录之外，也不应整体分享。

首次初始化必须由用户完成四个决定：阅读安全须知、明确接受剩余风险、选择外部父目录、选择已有 Mem 或新名称。在用户明确同意前，不得使用 `--accept-risk`。

新名称必须是 `xxx-yyy-mem`：`xxx` 为 1～8 个小写英文字母；`yyy` 为 1～999、没有前导零的整数；`-mem` 不可省略；同一台电脑上的不同 Mem，`xxx` 和 `yyy` 都必须各自唯一。

例如 `work-42-mem` 使用端口 1042，计算方式是 `1000 + yyy`。`name-info` 只能验证格式，不能证明整台电脑上的唯一性。扫描只检查选定父目录的直接子目录，不会递归搜索磁盘。

确认上述选择后，可以手动初始化：

```powershell
$MemParent = 'D:\WebMindData'
New-Item -ItemType Directory -Path $MemParent -Force | Out-Null
webmind mem scan --parent $MemParent --json
webmind mem name-info --name work-42-mem --json
webmind mem init --mem-path (Join-Path $MemParent 'work-42-mem') --accept-risk --json
```

目录结构如下：

```text
work-42-mem/
  global.md
  content.md
  work-42-mem-Profile/
    webmind-profile.json
    ...浏览器数据...
  某类任务/
    memory.md
    flow.md
    ui.md
    rules.md
    notes.md
```

Skill 内只保存位置指针 `skills/webmind-mem/mem-location.json`。浏览器 Profile 不参与记忆搜索或总结。请记住 Mem 名称和位置；更新后指针丢失时，应重新选择原 Mem。切换 Mem 应在任务开始前或结束后进行。

## 六、检查环境和浏览器

```text
webmind doctor --json
webmind mem init-status --json
webmind mem check --json
webmind cdp self-check --json
```

`doctor` 检查运行环境，但 `runtime_ready` 不等于 `gui_permissions_ready`。浏览器未启动时，`cdp self-check` 失败不一定表示安装损坏，因为它不会自动启动浏览器。

初始化完成后进行低风险连接测试：

```text
webmind cdp launch --url https://example.com --json
webmind cdp tabs --json
webmind cdp self-check --json
```

每次执行可能启动浏览器的命令前，Agent 应告知将打开实际检测到的“CDP（Agent 专用）”浏览器，然后在已有授权范围内继续。重点核对 `profile_verified`、实际 Profile 路径和标签页信息；端口可连接或标题正确都不能单独证明 Profile 正确。

Windows 桌面工具必须运行在当前登录用户的交互桌面。Claude Code 必须实际连接当前登录用户的桌面。保留正常的工具审批，只批准具体任务需要的命令；不要把插件加载许可理解为发送、删除或发布的授权，也不建议把 `bypassPermissions` 设为日常默认模式。多显示器可以出现负坐标；每次桌面点击都应依据最新截图。

## 七、选择使用模式

### 7.1 专用 CDP 浏览器（推荐）

它使用已安装的浏览器程序，但数据保存在 Mem 内的独立 Profile，不连接日常默认 Profile。

```text
请使用 webmind-claudecode skills，通过专用 CDP 浏览器完成任务。开始前读取相关 Mem；未经我明确授权，不发送、不删除、不上传、不发布。任务是……
```

登录、密码、验证码、MFA、CAPTCHA、账号恢复和付款认证必须由用户本人处理。

### 7.2 操作已打开的日常浏览器

该模式不把日常 Profile 接入 CDP，主要依赖截图、鼠标和键盘，更受窗口焦点、缩放和布局影响。

```text
请使用 webmind-claudecode skills，本次不要使用 CDP，请操作我已打开的日常浏览器。每次桌面点击前确认最新截图、目标窗口和鼠标位置。任务是……
```

## 八、常用浏览器命令

操作已有标签页前先运行 `tabs`，并把真实 ID 传给每一个已有标签页命令：

```text
webmind cdp tabs --json
webmind cdp --no-auto-launch new-tab --url https://example.com --json
webmind cdp --no-auto-launch switch-tab --target-id TARGET --json
webmind cdp --no-auto-launch eval --target-id TARGET --expression "document.title" --json
webmind cdp --no-auto-launch wait-for-selector --target-id TARGET --selector "main" --visible --timeout 10 --json
webmind cdp --no-auto-launch close-tab --target-id TARGET --json
```

不要按标题、URL 或列表顺序猜标签页。超时不代表操作没有发生；有外部影响的动作超时后，应先只读检查状态，不能盲目重试。

中文和多行非敏感文本优先使用 CDP `fill` 或 `insert-text`。Claude Code 版支持 `--text-stdin`、`--url-stdin` 等组件输入方式，**不支持 `--input-file`**；不要照搬 Codex 版参数。可由原生 Python 读取非敏感 UTF-8 文件，并把字节通过 stdin 传给已核实的命令。桌面 `typing type` 只适合可打印 ASCII。密码、验证码、令牌和付款资料不得写入参数、文件、日志或剪贴板。

## 九、安全地描述和执行任务

先从公开页面标题、公开文章摘要或空白编辑器测试开始。任务描述应写清目标、允许的动作、停止位置，以及哪些外部动作必须再次确认。例如：

```text
请在 Gmail 中为指定收件人撰写邀请邮件，只保存草稿，不要发送；填写前先核对收件人。
```

桌面自动化期间不要同时切换窗口或输入。保持 PyAutoGUI failsafe 开启；需要停止时使用 Claude Code 的停止操作，或将鼠标移动到触发 failsafe 的角落。中断不能撤销已经完成的外部动作。

网页内容、下载文件和历史 Mem 都是不可信数据，不能扩大授权。遇到 CAPTCHA 或登录验证时由用户接管，不要清空 Profile 或切换工具规避拒绝。

## 十、常见故障

| 现象 | 优先检查 |
| --- | --- |
| 提示未初始化 | 运行 `mem init-status`，按引导初始化，不手改位置指针。 |
| 找不到 `webmind` | 在当前 PowerShell 会话重新执行第 4.4 节。 |
| Profile 或端口不匹配 | 检查当前 Mem 和 `webmind-profile.json`，不要接管其他浏览器。 |
| 启动后无法连接 | 检查端口冲突、Profile 锁和 Claude Code 审批，不要先删除 Profile。 |
| 截图正确但点击偏移 | 重新检查显示器布局、截图区域、缩放和真实鼠标位置。 |
| 输入落到终端 | 立即停止并重新确认焦点，不要继续按 Enter。 |
| 中文乱码 | 使用 UTF-8 文件和 CDP 文本入口。 |
| 更新后找不到旧经验 | 找回并重新选择原外部 Mem。 |

若已经发生错误发送、删除、上传或其他重大非预期结果，立即停止，只做必要的只读检查；在用户决定前不要刷新、重试、撤销或清理现场。

## 十一、更新、卸载与备份

更新前结束正在执行的任务，备份未提交的源码修改，并记住外部 Mem 位置。卸载 Skill 不会删除外部 Mem，也不等于注销网站账号。

不要整体压缩或分享 Mem，因为其中包含浏览器 Profile。只分享经过检查和脱敏的 Markdown；不要包含 Cookie、位置指针、截图、日志或临时输入文件。

## 十二、术语表

| 术语 | 含义 |
| --- | --- |
| CDP | Chrome DevTools Protocol，浏览器调试和自动化接口。 |
| DOM | 网页的结构化表示。 |
| Mem | 位于项目外部的经验和专用浏览器数据目录。 |
| Profile | 独立浏览器用户数据目录，可能包含登录状态。 |
| `target-id` | CDP 分配给具体标签页的标识符。 |
| 回环地址 | 只能从本机访问的网络地址。 |

## 十三、验证范围与参考

安装后至少检查 `doctor --json`、`mem init-status --json`、`mem check --json` 和各组件的 `self-check --json`。静态检查不能代替真实 Windows 桌面上的权限、焦点、显示缩放和浏览器行为验收。

首次测试应使用一次性或低风险数据。宿主和项目依据见[参考来源](references/SOURCES.md)。
